---
name: deerphotos
license: MIT
description: 用用户指定的固定人物形象生成或编辑群体生活 AI 照片，识别十位成员的本名、别名、口语及多轮场景指令。适用于朋友聚会、旅行、吃饭、日常抓拍和群像场景映射；普通动物照片、普通词语或无关人物请求不触发。
---

# 鹿图DeerPhotos

将用户讲述的生活片段转译为固定角色出演的照片。每个参考形象代表一个现实中的人；本名和所有别名共用同一个角色身份。照片感描述的是摄影效果，不表示必须把动物或卡通角色改成人类，也不表示生成内容是实拍。

## 人物与形象

每次使用先读 [人物档案](references/characters.json)，按需读 [指令变体与示例](references/instruction-variants.md)。档案是人物和别名的唯一权威列表；以稳定 `id` 跟踪身份，以本名归一化。十位成员的本名、68 个别名及十张用户提供的参考图已绑定并随 skill 保存，`visual.status` 均为 `bound`。形象细节以对应原图为准，文字描述为辅助；新增成员仍需先绑定参考。

- 精确本名、完整别名优先，最长匹配优先，再结合句法和上下文。简繁写法、英文大小写（如 Tango/TANGO）、全半角及无意义空格可归一化，但保留用户原文。
- 同一人多次被不同称呼提及，合并为一个角色；除非用户明确要分身或重复形象。多个名字不能生成混合角色。
- 昵称是身份入口，不能据此猜外观、物种、性格或动作。“鹿子奇”是魏子奇，“鹿绍源”是詹绍源，不能被单字“鹿”抢先匹配。粗口昵称仅用于识别，不转成身体部位或画面文字。
- 常用词需要语境判断：“在展位拍照”“录一录视频”“打扑克”“下五子棋”通常是地点或动作，不自动添加对应成员。明确称呼、“叫上”、与人名并列等证据支持昵称解释。存在会影响出场名单的歧义时，只问必要的一句。
- 黎健毓的“寝委书记/书记/记记/记吧/黎书记/哆啦A梦/哆啦B梦/B梦/健叔”共用同一身份；英文 A/B 的大小写、全半角和名字内部无意义空格可归一化。群体场景里的昵称指向其已绑定形象，不改成普通无发无眼镜的哆啦A梦。“书记讲话”若没有群体语境可能是职务描述；“记吧”也可能是动词短语，不能仅按子串命中添加成员。明确请求原版动漫角色时遵从当次请求，不强制映射为黎健毓。
- 未收录的谐音、错别字、缩写只作为候选，不能自动永久加入别名。用户纠正后在本次会话采用；用户要求“记住/以后都叫”时更新档案，检查是否与别人冲突。

需要出场者尚无绑定图时，先检查用户当次提供的图和上下文。只在来源明示姓名、图中明确标签或用户指定的对应关系下绑定；不能靠脸部猜真实身份，也不能随意把本地无关图片配给某人。缺参考图或多人图无对应关系，向用户索取所缺图或对应关系；可先整理场景，但不冒充已完成角色一致的图片。

收到参考图后，查看图像，将原图复制到本 skill 的 `assets/characters/<id>/`，在档案 `references` 里记录相对路径、图的角色（主参考/补充参考）、来源和已知映射依据。记录可见外观与稳定识别点（物种/形态、脸型、毛色或肤色、发型、比例、固定配件）；不推断不可见细节。确认依据成立后改为 `bound`。一张群像可被多人共用，但为每个角色记录明确的位置/标签；保留原图，不用未经检查的自动裁图代替原始参考。

同一个人可以有多个参考角度。新图是这次造型还是长期新默认，以用户措辞决定；无法判断且影响以后身份时才问。更换服装、姿势、场景、光线一般不改变身份，单次变化不能覆盖长期档案。

## 场景理解

先在内部归纳：出场者、每人动作、互动对象、地点、时间、气氛、风格、构图和明确排除项。把现实人物名称映射到对应形象，保留事件中的动作和关系；只补足成像需要的自然细节，不凭空增加成员、地点事实、人物关系或新剧情。

- 指定几人就画几人；角色人数按唯一身份计。“全员”在此固定群体语境下指档案中全部成员（当前十人）；明确人数与名单不一致时澄清，不把“九个人”自动当成全员；“大家/我们/他们”优先继承最近明确名单，没有范围时澄清，不能把用户的“我”默认成某人。
- “A 和 B”是两位，“A 或 B”不是两位。“除了 A”排除 A；“别让 A 出镜”也排除 A。“不是 A，是 B”替换身份；“别动 A，换背景”保留 A。
- 区分拍摄者、电话另一头的人、被提及者和实际入镜者。“A 给 B 拍照”默认 B 入镜，不强制 A 也在画面中。
- 多轮“再来一张/还是他们/换个地方/其他不变”继承最近确认的身份和未被修改的场景条件；修改仅作用于指定部分。“这次不要龟龟，下次再带”只影响本次。
- 将“整一张/来张/拍个/P 一下/同款/像昨天那样”等按语义理解为生成或编辑请求，不要求用户按固定模板表达。不能从“昨天那样”编造昨天发生过什么；有历史参考则用，没有则问必要信息。

## 生成与修改

默认生成一张自然的生活照片：真实光线、合理透视、自然互动和材质细节。若参考主体为动物或卡通形象，保持其身份和形态，以摄影光影融入生活场景；不要擅自生成真人脸、人偶服或统一物种。用户明确指定插画、胶片、自拍、监控、表情包等风格时遵从。未要求时不加姓名、对白、水印或拼贴布局。保留默认服装时可以保留原有图案与文字，但参考中的生成平台水印不是角色特征，不带入新场景。不得把原图的香烟、相机、平板或背包强行塞入每个场景；根据活动决定是否携带。龟壳与鹿角属于身体识别点。涂腾辉的参考较写实，其余多为立体卡通：统一场景光线、透视与材质融合，不抹平各人的脸型、物种和体型差异。

优先使用可用的内置 `image_gen` 生图工具，遵循当前工具接口；不自行切换到付费 API 或要求密钥。若环境提供 imagegen skill，可读取其中与当前生成/编辑有关的规范。工具不可用时如实说明，交付可执行的提示词和所需参考图清单，不把提示词当作已经生成的照片。

调用前查看所需本地图像。将每张输入图明确标为角色参考、待编辑图或场景参考，按图像顺序逐一绑定身份，不能只把昵称丢给模型。全部目标都有本地路径时使用 `referenced_image_paths`；存在仅在对话内可用的目标时按当前工具文档选择能覆盖全部输入的方式，不能同时设置两种引用机制。若无法覆盖所有必要参考，请用户重新提供缺失图片。不能默默遗漏角色参考。

提示词按以下信息组织，省略无关空项：

```text
任务：用给定角色演绎这段生活场景，生成自然摄影效果的 AI 群体生活照片。
参考绑定：图 1 → <本名/稳定 id>；图 2 → <本名/稳定 id>……
出场名单：<唯一角色清单>，共 N 位；每个角色仅出现一次。
角色锁定：<每人的可见识别特征与必须保留的形态>。
场景与动作：<谁在哪里做什么，和谁互动，排除谁>。
摄影表达：<用户指定风格、构图、时间和气氛；适量默认细节>。
若为编辑：仅改变 <修改项>，保持 <人物及其他不变项>。
约束：不串脸、不互换物种/毛色/配件，不漏人或增添指定外成员。
```

生成后查看图像：核对人数、身份区分、动作归属、用户排除项及明显视觉缺陷。出现漏人、串角色或关键要求不符时做针对性修正；连续两次修正仍未解决，报告具体缺陷并交付当前最佳结果，不循环无止境或声称完全一致。参考图可提高一致性，但不要保证像素级锁定。

用户指定目录就保存到该目录；没有指定则遵循生图工具与当前工作区的保存约定。对最终选定图片同时保存简短场景记录（原始请求、实际出场者 id/本名、参考文件、最终提示词、版本、生成日期、`AI 生成` 来源）；未提供的事件日期不以生成日期冒充。编辑不覆盖原图，使用递增版本。展示最终照片并给出文件位置；仅在需要解释歧义或偏差时补充说明，不把技术细节塞进画面。
